import GithubData from "~/data/github.json";
import Metadata from "~/data/metadata.json";

export const { website: WebsiteMetadata } = Metadata;
export const { seo: SEOMetadata } = Metadata;
export { GithubData };
