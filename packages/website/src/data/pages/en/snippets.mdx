# Table of Content

- [CSS](#css)
  - [Set height 100% to a child element of a flex-row parent](#set-height-100-to-a-child-element-of-a-flex-row-parent)
  - [Text ellipsis](#text-ellipsis)
- [JavaScript](#javascript)
  - [Capitalize string](#convertToCapitalLetter-string)
  - [Copy to clipboard](#copy-to-clipboard)
  - [Create array with N elements](#create-array-with-n-elements)
  - [Delay](#delay)
  - [Focus and select the text inside the textarea](#focus-and-select-the-text-inside-the-textarea)
  - [Generate simple random string](#generate-simple-random-string)
  - [Regex examples](#regex-examples)
  - [Simple encrypt/decrypt](#simple-encrypt-decrypt)
  - [Slugify string](#generateSlug-string)
- [React](#react)
  - [Component to show the window resolution using Tailwind CSS classes](#component-to-show-the-window-resolution-using-tailwind-css-classes)
  - [Iterate a children prop](#iterate-a-children-prop)
- [Git](#git)
  - [Alias](#alias)
  - [Remote branches](#remote-branches)
  - [Stash](#stash)

---

# CSS

## Set height 100% to a child element of a flex-row parent

<Collapsible>
  <Code
    language="css"
    fileName="Set height 100% to a child element of flex-row parent"
    code={`.parent{
  align-items: stretch;
}
// or
.child{
  align-self: stretch;
}`}
  />
</Collapsible>

## Text ellipsis

<Collapsible>
  <Code
    language="css"
    fileName="Text ellipsis"
    code={`.ellipsis{
  text-overflow: ellipsis;
  white-space: nowrap;
  overflow: hidden;
}`}
  />
</Collapsible>

# JavaScript

## Capitalize string

<Collapsible>
  <Code
    language="typescript"
    fileName="Capitalize string"
    code={`function convertToCapitalLetter(str: string): string {
  return str
    .toLowerCase()
    .split(" ")
    .map(item => (item ? item[0].toUpperCase() + item.substring(1) : ""))
    .join(" ")
    .trim();
}`}
  />
</Collapsible>

## Copy to clipboard

<Collapsible>
  <Code
    language="typescript"
    fileName="Copy to clipboard"
    code={`async function copyToClipboard(
  e?: Record<string, any>,
  text?: string,
): Promise<any> {
  try {
    if (!e && !text) throw new Error("Invalid params");
    if (!navigator.clipboard) throw new Error("Clipboard not supported");
    let clipboardText = "";
    if (e) {
      const { currentTarget: element } = e;
      clipboardText = element.getAttribute("data-clipboard-text");
    } else if (text) {
      clipboardText = text;
    }
    navigator.clipboard.writeText(clipboardText).then(
      () => {
        console.log("Copying to clipboard was successful");
      },
      err => {
        console.error("Could not copy text:", err);
      },
    );
  } catch (e) {
    console.error(e);
  }
}`}
  />
</Collapsible>

## Create array with N elements

<Collapsible>
  <Code
    language="typescript"
    fileName="Create array with N elements"
    code={`function createArray(length: number): Array<number> {
  return Array.from(Array(length).keys()).map(value => value + 1);
}`}
  />
</Collapsible>

## Delay

<Collapsible>
  <Code
    language="typescript"
    fileName="Delay"
    code={`function delay(time: number): Promise<any> {
  return new Promise(resolve => {
    setTimeout(resolve, time);
  });
}`}
  />
</Collapsible>

## Focus and select the text inside the textarea

<Collapsible>
  <Code
    language="typescript"
    fileName="Delay"
    code={`const element = document.getElementById('my-textarea');
element.focus();
element.select();`}
  />
</Collapsible>

## Generate simple random string

<Collapsible>
  <Code
    language="javascript"
    fileName="Delay"
    code={`function makeSimpleRandomString(length) {
  let result = "";
  const characters = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
  const charactersLength = characters.length;
  for (var i = 0; i < length; i++) {
    result += characters.charAt(Math.floor(Math.random() * charactersLength));
  }
  return result;
}`}
  />
</Collapsible>

## Regex examples

<Collapsible>
  <Code
    language="javascript"
    fileName="Regex examples"
    code={`function escapeRegExp(text: string): string {
  return text.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&");
}
const replacement = "my";
"my string my string".replace(new RegExp(escapeRegExp(replacement), "g"), "your");
// output: "your string your string"
// -----
// /^(sm:|md:|lg:|xl:)?(tw-my-|tw-mx-|tw-mt-|tw-mb-)\d{1,2}$/
// ^ => starts with
// (x,y,z) => OR
// ? => optional
// /d{1,2} => numbers one or two digits
// $ => ends`}
  />
</Collapsible>

## Simple encrypt/decrypt

<Collapsible>
  <Code
    language="typescript"
    fileName="Simple encrypt/decrypt"
    code={`const { AES, enc } = require('crypto-js');
const { CRYPTO_KEY } = process.env;
function encrypt(str) {
  return AES.encrypt(str, CRYPTO_KEY).toString();
}
function decrypt(str) {
  const bytes = AES.decrypt(str, CRYPTO_KEY);
  return bytes.toString(enc.Utf8);
}`}
  />
</Collapsible>

## Slugify string

<Collapsible>
  <Code
    language="typescript"
    fileName="Slugify string"
    code={`function generateSlug(str: string): string {
  let result = str.replace(/^\s+|\s+$/g, "").toLowerCase();
  // remove accents, swap ñ for n, etc
  const FROM = "àáäâèéëêìíïîòóöôùúüûñç·/_,:;";
  const TO = "aaaaeeeeiiiioooouuuunc------";
  for (let i = 0, l = FROM.length; i < l; i += 1) {
    result = result.replace(new RegExp(FROM.charAt(i), "g"), TO.charAt(i));
  }
  result = result
    .replace(/[^a-z0-9 -]/g, "") // remove invalid chars
    .replace(/\s+/g, "-") // collapse whitespace and replace by -
    .replace(/-+/g, "-"); // collapse dashes
  return result;
}`}
  />
</Collapsible>

# React

## Component to show the window resolution using Tailwind CSS classes

<Collapsible>
  <Code
    language="jsx"
    fileName="Component to show the window resolution using Tailwind CSS classes"
    code={`import { useState, useLayoutEffect } from 'react';
const WindowSize = () => {
  const [size, setSize] = useState([0, 0]);
  useLayoutEffect(() => {
    const updateSize = () => {
      setSize([window.innerWidth, window.innerHeight]);
    };
    updateSize();
    window.addEventListener('resize', updateSize);
    return () => window.removeEventListener('resize', updateSize);
  }, []);
  return (
    <div className="root">
      <span>{size.join('x')} | </span>
      <span className="inline-block sm:hidden">mobile</span>{' '}
      <span className="hidden sm:inline-block md:hidden">sm</span>{' '}
      <span className="hidden md:inline-block lg:hidden">md</span>{' '}
      <span className="hidden lg:inline-block">lg</span>
      <style jsx>
        {\`
          .root {
            background-color: rgba(0, 0, 0, 0.5);
            bottom: 0;
            color: white;
            font-weight: bold;
            left: 0;
            padding: 10px;
            position: fixed;
          }
        \`}
      </style>
    </div>
  );
};`}
  />
</Collapsible>

## Iterate a children prop

<Collapsible>
  <Code
    language="jsx"
    fileName="Iterate a children prop"
    code={`<ul>
  {React.Children.map(children, child => {
    if (child.props.myProp === 'x') {
      return (
        <li>
          <span className="tw-font-bold tw-mr-1">☑</span>
          <span className="tw-line-through tw-italic tw-opacity-75">
            {child.props.children}
          </span>
        </li>
      );
    }
    return child;
  })}
</ul>`}
  />
</Collapsible>

# Git

## Alias

<Collapsible>
  <Code
    language="bash"
    fileName="Alias"
    code={`# superlog
git config --global alias.superlog "log --graph --abbrev-commit --decorate --date=relative --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all"
# alias with parameters
git config --global alias.pt-pull '!f() { git checkout development; git pull; git checkout task/PTRAK-$1; git pull; git merge development; }; f'`}
  />
</Collapsible>

## Remote branches

<Collapsible>
  <Code
    language="bash"
    fileName="Remote branches"
    code={`# show remote branches
git remote -v
# prune remote branches list
git remote update origin --prune`}
  />
</Collapsible>

## Stash

<Collapsible>
  <Code
    language="bash"
    fileName="Stash"
    code={`# Save a stash
git stash
# Restore all changes of the last stash
git stash apply
# Show all specific stash changes
git stash show stash@{0} -u
# Delete a specific stash
git stash drop -q stash@{0}
# Show all saved stashes
git stash list
# Restore all changes of a specific stash
git stash apply stash@{0}`}
  />
</Collapsible>
